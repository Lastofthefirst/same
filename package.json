{
  "name": "same-mouth-workspace",
  "version": "0.1.0",
  "description": "Same Mouth - Cooperative Farming Game (Tauri + PWA)",
  "private": true,
  "scripts": {
    "install:all": "pnpm install && cd shared && pnpm install && cd ../pwa && pnpm install && cd ../tauri-app && pnpm install",
    "build:shared": "cd shared && pnpm build",
    "build:pwa": "cd pwa && pnpm build", 
    "build:tauri": "cd tauri-app && pnpm build",
    "build:all": "pnpm run build:pwa && pnpm run build:tauri",
    "dev:pwa": "cd pwa && pnpm run dev",
    "dev:tauri": "cd tauri-app && pnpm run dev",
    "test:builds": "pnpm run build:all"
  },
  "workspaces": [
    "shared",
    "pwa", 
    "tauri-app"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
